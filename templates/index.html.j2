<!DOCTYPE html>
<html>
<head>
    <title>{{ website_title }}</title>
</head>
<body>
    <h1>{{ website_title }}</h1>
    <p>{{ website_message }}</p>
    <p>Server: {{ ansible_hostname }}</p>
</body>
</html>